[project]
name = "what-to-watch"
version = "0.1.0"
description = "Can't decide what to watch? Explore oscar nominated movies available on your streaming platform."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.43.0",
    "readchar>=4.2.2",
    "rich>=15.0.0",
    "simple-justwatch-python-api>=1.3.1",
]
